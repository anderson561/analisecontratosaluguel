[project]
name = "contract-parser"
version = "0.1.0"
description = "Leitor Inteligente de Contratos de Locação (AI Contract Parser)"
requires-python = ">=3.11"
dependencies = [
    "pymongo>=4.7",
    "pydantic>=2.7",
    "pytesseract>=0.3.10",
    "pymupdf>=1.24",
    "pdf2image>=1.17",
    "python-docx>=1.1",
    "openpyxl>=3.1",
    "customtkinter>=5.2",
    "reportlab>=4.2",
    "python-dotenv>=1.0",
    "rapidfuzz>=3.9",
]

[project.scripts]
contract-parser-envcheck = "contract_parser.infrastructure.environment_check:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-cov>=5.0",
    "ruff>=0.5",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra --cov=contract_parser --cov-report=term-missing"
markers = [
    "integration: testes que exigem servicos reais (ex.: MongoDB vivo); skip automatico se indisponivel",
]

[tool.ruff]
line-length = 100
target-version = "py311"
